# Supabase (public — safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Supabase (server-only, optional — see src/lib/db/server.ts).
# Not required for the MVP surface; org bootstrap uses the
# bootstrap_organization() SECURITY DEFINER RPC instead.
# SUPABASE_SERVICE_ROLE_KEY=

# LLM provider key(s) used by the AI summary/chat routes.
NVIDIA_API_KEY=
