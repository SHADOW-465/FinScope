/**
 * Placeholder Supabase database types. Regenerate after every schema change:
 *
 *   npx supabase gen types typescript --project-id <ref> > src/lib/db/database.types.ts
 *
 * Kept as `any`-shaped until the first migration is applied and real types
 * are generated, so `createClient<Database>()` compiles either way.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
